.navigation__menu {
  display: flex;
  margin-right: 20px;

  &:last-child {
    margin-right: 0;
  }
}

.navigation__link {
  font-family: "open-sans-regular";
  font-size: 16px;
  color: $nav_menu-color;
  white-space: nowrap;
}