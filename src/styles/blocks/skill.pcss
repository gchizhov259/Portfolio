.skill {
  position: relative;
  width: 150px;
}

.skills__item {
  margin-right: 20px;
}

.skill__title {
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
  text-align: center;
  font-family: "open-sans-bo";
  font-size: 18px;
  opacity: 0.5;
}
